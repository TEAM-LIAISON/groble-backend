tasks.named('bootJar') {
    enabled = false
}
tasks.named('jar') {
    enabled = true
}


dependencies {
    // groble-core 모듈 의존성
    implementation project(':groble-core')

    // groble-auth 모듈 의존성
    implementation project(':groble-auth')
}

